apiVersion: v1
kind: Secret
metadata: { name: postgres, namespace: app }
stringData:
  POSTGRES_USER: app
  POSTGRES_PASSWORD: CHANGE-ME-postgres-password    # override per-overlay via secretGenerator
  POSTGRES_DB: app
---
apiVersion: v1
kind: Service
metadata: { name: postgres, namespace: app }
spec:
  clusterIP: None
  selector: { app: postgres }
  ports: [{ port: 5432, name: pg }]
---
apiVersion: apps/v1
kind: StatefulSet
metadata: { name: postgres, namespace: app }
spec:
  serviceName: postgres
  replicas: 1
  selector: { matchLabels: { app: postgres } }
  template:
    metadata: { labels: { app: postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom: [{ secretRef: { name: postgres } }]
          ports: [{ containerPort: 5432 }]
          volumeMounts:
            - { name: data, mountPath: /var/lib/postgresql/data, subPath: pgdata }
          readinessProbe: { exec: { command: [pg_isready, -U, app, -d, app] }, periodSeconds: 3 }
          livenessProbe:  { exec: { command: [pg_isready, -U, app, -d, app] }, periodSeconds: 20, initialDelaySeconds: 30 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        # storageClassName intentionally omitted — each overlay patches this.
        accessModes: [ReadWriteOnce]
        resources: { requests: { storage: 10Gi } }
